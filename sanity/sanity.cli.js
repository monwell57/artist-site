import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    // projectId: '4hglk3si',
    // dataset: 'artist-site-march'
    projectId: '4hglk3si',
    dataset: 'production',
  },
})
